name: Image
run-name: build image and provenance
on:
  push:
  
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  set-vars:
    permissions:
      contents: read
    if: github.repository == 'sohgaura/creditFLY'
    runs-on: ubuntu-22.04
    outputs:
      image-tag: ${{ steps.image.outputs.tag}}
      platforms: ${{ steps.platforms.outputs.platforms }}
    steps:
      - uses: actions/checkout@8410ad0602e1e429cee44a835ae9f77f654a6694 # v4.0.0

      - name: Set image tag for ghcr
        run: echo "tag=$(cat ./VERSION)-${GITHUB_SHA::8}" >> $GITHUB_OUTPUT
        id: image

      - name: Determine image platforms to use
        id: platforms
        run: |
          IMAGE_PLATFORMS=linux/amd64
          if [[ "${{ github.event_name }}" == "push" || "${{ contains(github.event.pull_request.labels.*.name, 'test-multi-image') }}" == "true" ]]
          then
            IMAGE_PLATFORMS=linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
          fi
          echo "Building image for platforms: $IMAGE_PLATFORMS"
          echo "platforms=$IMAGE_PLATFORMS" >> $GITHUB_OUTPUT

         
  
  build-only:
    needs: [set-vars]
    permissions:
      contents: read
      packages: write  # for pushing packages to GHCR, which is used by cd.apps.argoproj.io to avoid polluting Quay with tags
      id-token: write # for creating OIDC tokens for signing.
    if: ${{ github.repository == 'sohgaura/creditFLY' && github.event_name != 'push' }}
    uses: ./.github/workflows/image-reuse.yaml
    with:
      # Note: cannot use env variables to set go-version (https://docs.github.com/en/actions/using-workflows/reusing-workflows#limitations)
      # renovate: datasource=golang-version packageName=golang
      push: false

  build-and-publish:
    needs: [set-vars]
    permissions:
      contents: read
      packages: write  # for pushing packages to GHCR, which is used by cd.apps.argoproj.io to avoid polluting Quay with tags
      id-token: write # for creating OIDC tokens for signing.
    if: ${{ github.repository == 'sohgaura/creditFLY' && github.event_name == 'push' }}
    uses: ./.github/workflows/image-reuse.yaml
    with:
      # docker_image_name: docker.io/dockerdk/creditfly:provenance
      ghcr_image_name: ghcr.io/sohgaura/creditfly/creditfly:provenance
      # docker_image_name: docker.io/dockerdk/creditfly:${{ needs.set-vars.outputs.image-tag }}
      # Note: cannot use env variables to set go-version (https://docs.github.com/en/actions/using-workflows/reusing-workflows#limitations)
      # renovate: datasource=golang-version packageName=golang
      push: true
    secrets:
      # docker_username: ${{ vars.DOCKERDK_USERNAME }}
      # docker_password: ${{ secrets.DOCKERDK_TOKEN }}
      ghcr_username: ${{ github.actor }}
      ghcr_password: ${{ secrets.GITHUB_TOKEN }}

  build-and-publish-provenance: # Push attestations to GHCR, latest image is polluting quay.io
    needs:
      - build-and-publish
    permissions:
      actions: read # for detecting the Github Actions environment.
      id-token: write # for creating OIDC tokens for signing.
      packages: write # for uploading attestations. (https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/container/README.md#known-issues)
    if: ${{ github.repository == 'sohgaura/creditFLY' && github.event_name == 'push' }}
    # Must be refernced by a tag. https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/container/README.md#referencing-the-slsa-generator
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.0.0
    with:
      image: ghcr.io/sohgaura/creditfly/creditfly:provenance
      digest: ${{ needs.build-and-publish.outputs.image-digest }}
      registry-username: ${{ vars.DOCKERDK_USERNAME }}
    secrets:
      registry-password: ${{ secrets.GH_TOKEN }}
      # image: docker.io/dockerdk/creditfly:provenance
      # digest: ${{ needs.build-and-publish.outputs.image-digest }}
      # registry-username: ${{ vars.DOCKERDK_USERNAME }}
    # secrets:
      # registry-password: ${{ secrets.DOCKERDK_TOKEN }}

  generate-hash: 
    needs: 
      - build-and-publish
    permissions:
      actions: read # for detecting the Github Actions environment
      id-token: write # Needed for provenance signing and ID
      contents: write #  Needed for release uploads
    if: ${{ github.repository == 'sohgaura/creditFLY' }}
    runs-on: ubuntu-22.04
    outputs:
      hashes: ${{ steps.hashes.outputs.hashes}}
    steps:
      - name: Determine hashes for provenance
        id: hashes
        run: |
          hashes=$(echo creditfly  ${{ needs.build-and-publish.outputs.image-digest }}  | sed s/^sha256:// | base64 -w0)
          echo "hashes=$hashes" >> $GITHUB_OUTPUT

  npm-build:
    outputs:
      digest: ${{ steps.hash.outputs.digest }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # - name: Set up Node.js (for Node.js project)
      #   if: matrix.language == 'nodejs'
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: 18
      
      # - name: "build artifacts"
      #   run: |
      #     # These are some amazing artifacts.
      #     # echo "foo" > artifact1
      #     # echo "bar" > artifact2
      #     npm install
      #     npm run build --silent --no-eslint-rule no-lone-blocks
          
      - name: "generate hash"
        shell: bash
        id: hash
        run: |
          # sha256sum generates sha256 hash for all artifacts.
          # base64 -w0 encodes to base64 and outputs on a single line.
          # sha256sum artifact1 artifact2 ... | base64 -w0
          # echo "digest=$(sha256sum artifact1 artifact2 | base64 -w0)" >> "${GITHUB_OUTPUT}"
          # echo "digest=$(echo -n "$(cat package-lock.json)" | base64 -w0)" >> "${GITHUB_OUTPUT}"
          digest=$(echo -n "$(cat package-lock.json)" | base64 -w0)
          echo "digest=${digest}" >> "$GITHUB_OUTPUT"
 
  generic-provenance:
    needs: 
      - build-and-publish
      - generate-hash
      - npm-build
    permissions:
      actions: read # for detecting the Github Actions environment
      id-token: write # Needed for provenance signing and ID
      contents: write #  Needed for release uploads
    # if: ${{ github.repository == 'sohgaura/creditFLY' }}
    # runs-on: ubuntu-22.04
    # Must be refernced by a tag. https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/container/README.md#referencing-the-slsa-generator
    # steps:
    #   - name: generate-hash
    #     id: generate-hash
    #     run: |
    #       hashes=$(echo ${{ 'needs.build-and-publish.outputs.image-digest'}} | jq --raw-output '.[] | {name, "digest": (.extra.Digest // .extra.Checksum)} | select(.digest) | {digest} + {name} | join("  ") | sub("^sha256:";"")' | base64 -w0)
    #       echo "hashes=$hashes" >> $GITHUB_OUTPUT

      # - name: generate-provenance
      #   id: generate-provenance
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.0.0
    with:
      # base64-subjects: "${{ steps.rgenerate-hash.outputs.hashes }}"
      # base64-subjects: "${{ needs.generate-hash.outputs.hashes}}" 
      base64-subjects: "${{ needs.npm-build.outputs.digest}}" 
      # base64-subjects: |
      #   [
      #     {
      #       "digest": "${{ needs.build-and-publish.outputs.image-digest }}",
      #       "name": "creditfly"
      #     }
      #   ] 
      provenance-name: "npm.jsonl"
      upload-assets: true
